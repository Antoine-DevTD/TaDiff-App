import { EmptyState } from "@/components/ui/empty-state";

export default function SettingsPage() {
  return (
    <EmptyState
      title="Parametres prets"
      description="Les reglages equipe, facturation et integrations arriveront apres le socle auth."
    />
  );
}
