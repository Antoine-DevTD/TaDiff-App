import { EmptyState } from "@/components/ui/empty-state";

export default function ContractsPage() {
  return (
    <EmptyState
      title="Contrats prets"
      description="La gestion des contrats attendra le schema Supabase et le stockage documentaire."
    />
  );
}
