import { EmptyState } from "@/components/ui/empty-state";

export default function PipelinePage() {
  return (
    <EmptyState
      title="Pipeline commercial pret"
      description="Le kanban dnd-kit sera implemente dans un lot dedie, avec colonnes et opportunites."
    />
  );
}
