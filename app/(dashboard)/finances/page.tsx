import { EmptyState } from "@/components/ui/empty-state";

export default function FinancesPage() {
  return (
    <EmptyState
      title="Finances simples pretes"
      description="Les budgets, revenus previsionnels et exports seront structures dans un prochain lot."
    />
  );
}
