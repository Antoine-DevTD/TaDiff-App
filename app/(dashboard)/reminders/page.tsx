import { EmptyState } from "@/components/ui/empty-state";

export default function RemindersPage() {
  return (
    <EmptyState
      title="Relances pretes"
      description="Les relances seront reliees aux contacts, spectacles et opportunites apres connexion des donnees."
    />
  );
}
